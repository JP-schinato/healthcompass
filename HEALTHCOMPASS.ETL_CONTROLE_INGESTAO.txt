CREATE TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" 
   (	"ID_INGESTAO" NUMBER GENERATED ALWAYS AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE , 
	"DATASET" VARCHAR2(100 BYTE) COLLATE "USING_NLS_COMP", 
	"ANO" NUMBER(4,0), 
	"MES" NUMBER(2,0), 
	"ARQUIVO" VARCHAR2(500 BYTE) COLLATE "USING_NLS_COMP", 
	"QTD_REGISTROS" NUMBER, 
	"STATUS" VARCHAR2(20 BYTE) COLLATE "USING_NLS_COMP" DEFAULT 'PENDING', 
	"DATA_INICIO" TIMESTAMP (6), 
	"DATA_FIM" TIMESTAMP (6), 
	"MENSAGEM_ERRO" VARCHAR2(4000 BYTE) COLLATE "USING_NLS_COMP", 
	"DATA_CRIACAO" TIMESTAMP (6) DEFAULT SYSTIMESTAMP, 
	"DATA_ATUALIZACAO" TIMESTAMP (6) DEFAULT SYSTIMESTAMP
   )  DEFAULT COLLATION "USING_NLS_COMP" ;

CREATE UNIQUE INDEX "HEALTHCOMPASS"."PK_ETL_CONTROLE_INGESTAO" ON "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" ("ID_INGESTAO") 
  ;

CREATE UNIQUE INDEX "HEALTHCOMPASS"."UK_ETL_CONTROLE_INGESTAO" ON "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" ("DATASET", "ANO", "MES") 
  ;

ALTER TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" MODIFY ("ID_INGESTAO" NOT NULL ENABLE);

ALTER TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" MODIFY ("DATASET" NOT NULL ENABLE);

ALTER TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" MODIFY ("ANO" NOT NULL ENABLE);

ALTER TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" MODIFY ("MES" NOT NULL ENABLE);

ALTER TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" MODIFY ("STATUS" NOT NULL ENABLE);

ALTER TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" MODIFY ("DATA_CRIACAO" NOT NULL ENABLE);

ALTER TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" MODIFY ("DATA_ATUALIZACAO" NOT NULL ENABLE);

ALTER TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" ADD CONSTRAINT "CK_ETL_INGESTAO_STATUS" CHECK (STATUS IN ('PENDING','RUNNING','SUCCESS','ERROR')) ENABLE;

ALTER TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" ADD CONSTRAINT "CK_ETL_INGESTAO_MES" CHECK (MES BETWEEN 1 AND 12) ENABLE;

ALTER TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" ADD CONSTRAINT "PK_ETL_CONTROLE_INGESTAO" PRIMARY KEY ("ID_INGESTAO")
  USING INDEX  ENABLE;

ALTER TABLE "HEALTHCOMPASS"."ETL_CONTROLE_INGESTAO" ADD CONSTRAINT "UK_ETL_CONTROLE_INGESTAO" UNIQUE ("DATASET", "ANO", "MES")
  USING INDEX  ENABLE;

